package passing_returning_non_primitives;

public class passing_and_returning_both 
{
	/*
	 * String s= view(10, new Student())
	 * public String view(int a, Student s1)
	 * return "Hello"
	 * 
	 * Boolean b=disp(new Student(), new Student())
	 * public Boolean disp(Student s1, Student s2)
	 * return true;
	 * 
	 * */
}
